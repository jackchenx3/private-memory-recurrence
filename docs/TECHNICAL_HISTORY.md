# Retained technical corrections and limits

Study 050 completed its scientific allocation without a scientific-source or input correction. A subsequent local validator initially required bitwise-identical Linux/macOS logarithms and failed on a one-unit-in-the-last-place clock difference. A separate portable check retained the source and failure records, allowed the stated narrow clock tolerance, and required exactly identical ordered survivors. Across 17,694,720 clocks, 11,916 differed by one unit in the last place; none exceeded it. No scientific path was rerun and no endpoint was changed.

Study 054 required correction of an assessment label that confused the initial frequency difference with the difference in frequency changes. The numerical tables and criteria were unchanged; the original assessment remains in the internal archive. The manuscript distinguishes initial frequency, frequency change and final frequency explicitly.

For study 055, a draft runner integration issue was corrected before scientific-source freeze. There was no frozen scientific-source correction or rerun. The final reviewer delivery check initially overlapped an executor status-receipt update recording the pause. A fresh check against the stable receipt passed with unchanged scientific source and result manifests. All 1,152 preparations and 3,456 transfers passed independent saved-record reconstruction.

These records do not erase technical failures, convert them into scientific replications, or imply that a passing aggregate check certifies every omitted raw record. The complete raw execution archive remains separate from this compact public release.

Studies 056 and 057 each had a report-only percent-formatting failure after numerical completion. Frozen report.py is preserved alongside the separate report_display_fix.py and REPORT_FORMAT_REPAIR.json. The repair escaped literal percent signs for display; no scientific operator, seed, path, estimate or outcome classification changed. The 056 pre-submission compatibility correction and the 057 accepted-operator checks remain in the authenticated source archives.

Study 058 was implemented by the execution task and run by the supervisor through the same frozen, idempotent controller because executor SSH access was unavailable. No source or scientific design changed during recovery. Both raw-record and supervisor arithmetic audits passed. Publication figures 14/15 reorganize the same saved estimates for readable manuscript labels; their 46 numerical bindings are supplied.

Study059 used the frozen executor handoff and supervisor HPC access for one job. The saved-output audit passed all36 estimates and preselected raw block0. Figure16 reorganizes six saved mean/interval pairs; first-update pulse/continuous identities are structural zeros and are not empirical equivalence findings. Published source is archived for inspection; launch/control and full raw input archives are omitted.
